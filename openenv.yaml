name: pr-priority-pilot
version: 1.0
description: Three‑task prioritizer with guaranteed (0,1) scores
actions: Discrete(3)
observations:
  title: string
  description: string
tasks:
  - easy
  - medium
  - hard
endpoints:
  reset: /reset
  step: /step
  state: /state
